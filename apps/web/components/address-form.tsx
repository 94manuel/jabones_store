'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export function AddressForm() { const router=useRouter();const [open,setOpen]=useState(false);const [msg,setMsg]=useState(''); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget).entries());const response=await fetch('/api/backend/users/me/addresses',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...body,isDefault:true,country:'CO'})}); if(response.ok){setMsg('Dirección guardada.');setOpen(false);router.refresh();}else setMsg('No fue posible guardar la dirección.');}
 return <div style={{marginTop:18}}>{!open && <button className="btn-secondary" onClick={()=>setOpen(true)}>Agregar dirección</button>}{open && <form className="form-grid" onSubmit={submit}><div className="field"><label>Etiqueta</label><input name="label" defaultValue="Principal" required/></div><div className="field"><label>Recibe</label><input name="receiver" required/></div><div className="field full"><label>Dirección</label><input name="line1" required/></div><div className="field"><label>Ciudad</label><input name="city" required/></div><div className="field"><label>Departamento</label><input name="region" required/></div><div className="field full"><label>Teléfono</label><input name="phone" required/></div><button className="btn-primary">Guardar</button></form>}{msg && <p className="alert">{msg}</p>}</div>;
}
